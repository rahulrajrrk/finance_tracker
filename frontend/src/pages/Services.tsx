import React from 'react';
import { Typography } from '@mui/material';

export default function Services() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Services
      </Typography>
      <Typography>
        Define services and their profit logic (unit or lump sum). Set selling rates and base costs as required.
      </Typography>
    </div>
  );
}
